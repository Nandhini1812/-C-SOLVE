#include <stdio.h>
int main() {
    int num1,num2;
    char operators;
    scanf("%d %c %d",&num1,&operators,&num2);
    switch(operators){
        case '+' : printf("%d",num1+num2);break;
        case '-' : printf("%d",num1-num2);break;
        case '*' : printf("%d",num1*num2);break;
        case '/' : printf("%d",num1/num2);break;
        case '%' : printf("%d",num1%num2);break;
        defalut : printf("Enter the valid operator");
    }
    return 0;
}