#include <stdio.h>
int main() {
    int num;
    printf("Enter the number: ");
    scanf("%d", &num);
    
    if (num >= 100 && num <= 999)
    {
        printf("Three-digit number");
    } 
   
    else 
    {
        printf("NON-Three digit");
    }
    return 0;
}