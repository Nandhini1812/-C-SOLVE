#include <stdio.h>
int main() {
    int i, j;
    int rows = 5;
    for (i = rows; i >= 1; --i) {
        for (j = 0; j < i; ++j) {
            printf("%c", 'A' + j);
        }
        printf("\n");
    }
    return 0;
}