#include <stdio.h>
int main() {
    int a;
    printf("Enter the Age: ");
    scanf("%d", &a);
    
    if (a >= 18)
    {
        printf(" Eligible to vote");
    } 
   
    else 
    {
        printf("Not Eligible");
    }
    return 0;
}