#include <stdio.h>
int main() {
    int a;
    printf("Enter the Mark: ");
    scanf("%d", &a);
    
    if (a >= 75 && a<=90)
    {
        printf("Grade B");
    } 
 else {
     printf("Not Grade B");
 }
    return 0;
}