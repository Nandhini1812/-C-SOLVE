#include <stdio.h>
int main() {
    int n, count = 0, temp;
    scanf("%d" , &n);
    temp = n;
    for(; temp != 0; temp = temp / 10) {
        count++;
    }
    printf("%d" , count);
    return 0;
}