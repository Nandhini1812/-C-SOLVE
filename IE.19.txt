#include <stdio.h>
int main() {
    char a;
    scanf("%c",&a);
    if (a>=48 && a<=57)
    {
        printf("DIGIT");
    }
    else
    {
        printf("not in DIGIT");
    }
    return 0;
}